model FactRegister {
    regisId             Int           @id @default(autoincrement())
    studentId           String
    creditRequireId     Int
    subjectCourseId     Int
    gradeNumber         Float?        @db.Float
    gradeCharacter      String?       @db.VarChar(45)
    creditRegis         Int
    studyYearInRegis    Int
    studyTermInRegis    Int
    semesterYearInRegis Int
    semesterPartInRegis String        @db.VarChar(45)
    gradeLabelId        Int?
    grade_label         GradeLabel?   @relation(fields: [gradeLabelId], references: [gradeLabelId], onDelete: NoAction, onUpdate: NoAction, map: "fk_fact_register_grade_label1")
    credit_require      CreditRequire @relation(fields: [creditRequireId], references: [creditRequireId], onDelete: NoAction, onUpdate: NoAction, map: "fk_register_credit-require1")
    student             Student       @relation(fields: [studentId], references: [studentId], onDelete: NoAction, onUpdate: NoAction, map: "fk_register_student1")
    subject_course      SubjectCourse @relation(fields: [subjectCourseId], references: [subjectCourseId], onDelete: NoAction, onUpdate: NoAction, map: "fk_register_sublect-course1")

    @@index([gradeLabelId], map: "fk_fact_register_grade_label1_idx")
    @@index([creditRequireId], map: "fk_register_credit_require1_idx")
    @@index([studentId], map: "fk_register_student1_idx")
    @@index([subjectCourseId], map: "fk_register_sublect_course1_idx")
    @@map("fact_register")
}
